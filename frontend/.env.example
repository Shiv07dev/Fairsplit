# Backend API URL (set this in Netlify environment variables)
# For local development, leave empty (Vite proxy handles /api)
# For production, set to your deployed backend URL e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
